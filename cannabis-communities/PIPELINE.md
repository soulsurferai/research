# Pipeline

This report folder contains a curated subset of outputs for the Cannabis Communities analysis.

The analysis was generated using the Topics pipeline:

- `/_pipelines/topics-v1/`

That pipeline is included once (shared) to avoid duplicating agnostic scripts across multiple report folders.
